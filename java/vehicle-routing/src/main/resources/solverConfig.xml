<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">

  <solutionClass>org.acme.vehiclerouting.domain.VehicleRoutePlan</solutionClass>
  <entityClass>org.acme.vehiclerouting.domain.Vehicle</entityClass>
  <entityClass>org.acme.vehiclerouting.domain.Visit</entityClass>

  <scoreDirectorFactory>
    <constraintProviderClass>org.acme.vehiclerouting.solver.VehicleRoutingConstraintProvider</constraintProviderClass>
  </scoreDirectorFactory>

<termination>
  <spentLimit>PT90S</spentLimit>
  <unimprovedSpentLimit>PT25S</unimprovedSpentLimit>
</termination>

<constructionHeuristic/>

<!-- Phase 1 diversify -->
<localSearch>
  <acceptor>
    <lateAcceptanceSize>600</lateAcceptanceSize>
  </acceptor>
  <forager>
    <acceptedCountLimit>1</acceptedCountLimit>
  </forager>
  <moveSelector>
    <unionMoveSelector>
      <listChangeMoveSelector/>       <!-- relocate -->
      <listSwapMoveSelector/>         <!-- 1 1 swap -->
      <subListChangeMoveSelector/>    <!-- segment insert -->
      <subListSwapMoveSelector/>      <!-- cross exchange -->
    </unionMoveSelector>
  </moveSelector>
</localSearch>

<!-- Phase 2 intensify -->
<localSearch>
  <acceptor>
    <entityTabuSize>max(3, ceil(customers/6))</entityTabuSize>
    <moveTabuSize>max(3, ceil(customers/8))</moveTabuSize>
  </acceptor>
  <forager>
    <acceptedCountLimit>1</acceptedCountLimit>
  </forager>
  <moveSelector>
    <unionMoveSelector>
      <listChangeMoveSelector/>
      <listSwapMoveSelector/>
      <subListChangeMoveSelector/>
      <subListSwapMoveSelector/>
    </unionMoveSelector>
  </moveSelector>
</localSearch>
